# ComfyUI Easy Setup

Автоматизированный скрипт установки ComfyUI на Linux с поддержкой NVIDIA GPU.

## 🚀 Быстрый запуск

### Запуск напрямую из GitHub (без скачивания)

```bash
curl -fsSL https://raw.githubusercontent.com/Lemmur/ComfyUI-EasySetup/main/install.sh | bash
```

Или с использованием wget:

```bash
wget -qO- https://raw.githubusercontent.com/Lemmur/ComfyUI-EasySetup/main/install.sh | bash
```

### С опциями

**Установка с автоматическим запуском ComfyUI:**
```bash
curl -fsSL https://raw.githubusercontent.com/Lemmur/ComfyUI-EasySetup/main/install.sh | bash -s -- --simple-start
```

**Создать systemd сервис:**
```bash
curl -fsSL https://raw.githubusercontent.com/Lemmur/ComfyUI-EasySetup/main/install.sh | bash -s -- --service
```

**Создать сервис и сразу запустить:**
```bash
curl -fsSL https://raw.githubusercontent.com/Lemmur/ComfyUI-EasySetup/main/install.sh | bash -s -- --service --start
```

## 📋 Требования

- **ОС:** Ubuntu/Debian Linux
- **GPU:** NVIDIA с поддержкой CUDA
- **Права:** sudo (для установки драйверов и пакетов)

## 🔧 Опции скрипта

| Опция | Описание |
|-------|----------|
| `--simple-start` | Запустить ComfyUI сразу после установки (без systemd) |
| `--service` | Создать systemd сервис для автозапуска |
| `--start` | Запустить ComfyUI сразу после установки (требует --service) |

## 📝 Что делает скрипт

1. Обновляет системные пакеты
2. Устанавливает драйвер NVIDIA (если требуется)
3. Создаёт виртуальное окружение Python
4. Клонирует репозиторий ComfyUI
5. Устанавливает кастомные ноды (ComfyUI-Manager, ComfyUI-Downloader, Civicomfy, ComfyUI-Crystools)
6. Устанавливает CUDA Toolkit 13.0
7. Устанавливает PyTorch 2.9.1 с поддержкой CUDA 13.0
8. Сборка и установка SageAttention v2.2.0
9. Создаёт скрипт запуска `~/run_comfyui.sh` с флагом `--use-sage-attention`
10. (опционально) Создаёт systemd сервис

## ⚠️ Важно

- После установки драйвера NVIDIA может потребоваться перезагрузка системы
- Скрипт автоматически определит модель GPU и выберет подходящий драйвер
- Для работы требуется минимум 8 ГБ видеопамяти

## 🐛 Устранение проблем

Если скрипт не запускается напрямую из GitHub, убедитесь что:
- Установлен `curl` или `wget`
- Есть доступ к интернету
- У вас есть права sudo

```bash
sudo apt install curl wget
```
